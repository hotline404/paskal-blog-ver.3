import { size } from "../size/size";
import { color } from "../color/color";
import { position } from "../position/position";

export const mixtureStyle = {
  base_note: `${size.width.base_note} ${size.height.base_note} ${color.background.base_note} ${position.base_note}`,
  page: `${size.width.page} ${size.height.page} ${color.pageColor.base_note}`,
};
